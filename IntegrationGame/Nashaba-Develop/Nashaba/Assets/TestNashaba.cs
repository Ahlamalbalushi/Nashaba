using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TestNashaba : MonoBehaviour
{
    public NashabaScript nashaba;
    // Start is called before the first frame update
    void Start()
    {
        nashaba.setShooter();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
