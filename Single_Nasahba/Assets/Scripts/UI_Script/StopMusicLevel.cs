using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class StopMusicLevel : MonoBehaviour
{

    private void Awake()
    {
        //Destroy(StartGameMusic.Instance.gameObject.GetComponent<AudioSource>());
//        StartGameMusic.Instance.gameObject.GetComponent<AudioSource>().Play();
    }
    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
